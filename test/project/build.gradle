plugins {
    id("java")
}

sourceSets {
    main {
        java.srcDir("source")
    }
}

repositories {
	maven {url = file("build/repository")}
	mavenCentral()
}

dependencies {
    annotationProcessor("net.auoeke:uncheck:latest.release")
}
